export type CardType = {
  id: number,
  title: string,
  tagline:string,
  voteAverage: number,
  voteCount: number,
  releaseDate: string,
  posterPath: string,
  overview: string,
  budget: number,
  revenue: number,
  genres: Array<string>,
  runtime: null,
};
